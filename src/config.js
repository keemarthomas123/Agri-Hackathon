// API Configuration
// This will use the environment variable if set, otherwise fallback to localhost
export const API_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000';

export default API_URL;
